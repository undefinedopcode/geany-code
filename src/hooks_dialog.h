/*
 * hooks_dialog.h — Manage Claude Code hooks (PreToolUse, etc.)
 */

#ifndef HOOKS_DIALOG_H
#define HOOKS_DIALOG_H

#include <gtk/gtk.h>

/* Show the hooks management dialog. */
void hooks_dialog_run(GtkWindow *parent);

#endif /* HOOKS_DIALOG_H */
